services:
	nginx:
		image:
			nginx:mainline
		ports:
			443:44
		volumes:
			wp_data
	mariadb:
		image:
			mariadb:mainline
		volumes:
			db_data
		network:
			inception_network
	wordpress:
		image:
			wordpress:mainline
		volumes:
			wp_data
		network:
			inception_network
volumes:
	wp_data:
		driver: 		local
		name	:			wp_data
		mountpoint:	/var/www/
	db_data:
		driver: 		local
		name	:			wp_data
		mountpoint:	/var/data


networks:
	inception_network:
		driver: bridge
		name: inception_network